@model List<Ders>

<div class="container text-center">
    <div class="row">

        @foreach (Ders item in Model)
        {
           @await Html.PartialAsync("_DersCard",item);
        }
    </div>
</div>